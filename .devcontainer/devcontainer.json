// For format details, see https://aka.ms/vscode-remote/devcontainer.json or this file's README at:
// https://github.com/microsoft/vscode-dev-containers/tree/v0.202.3/containers/python-3
{
  "name": "azure-vision-api",
  "workspaceFolder": "/app/.devcontainer/.vscode",
  "dockerComposeFile": "/app/compose.yaml",
  "service": "azure-vision-runner",
    
  // Configure tool-specific properties.
  "customizations": {
    // Configure properties specific to VS Code.
    "vscode": {
      // Add the IDs of extensions you want installed when the container is created.
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "eamodio.gitlens",
        "KnisterPeter.vscode-github"
      ]
    }
  },

  // Comment out to connect as root instead. More info: https://aka.ms/vscode-remote/containers/non-root.
  "remoteUser": "appuser"
}
